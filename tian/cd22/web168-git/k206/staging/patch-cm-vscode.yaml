apiVersion: apps/v1
kind: Deployment
metadata:
  name: vscode205
spec:
  template:
    spec:
      volumes:
        - name: cm-init-sh
          configMap:
            name: cm-init-sh-206
            defaultMode: 0755